def soma_impares(n: int) -> int:
    soma = 0
    for i in range(1, n + 1):
        soma += 2 * i - 1
    return soma

for i in range(1, 1000):
    soma = soma_impares(i)
    if soma == i ** 2:
        print(f"n: {i} --> {soma} = {i**2}")
